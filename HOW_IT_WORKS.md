# ⚡️ JoAcademy Talent Finder - Technical Guide
## دليل نظام مطابقة الوظائف الذكي

هذا الملف يشرح آلية عمل النظام والتقنيات المستخدمة وكيفية تشغيله.

---

### 1. ما هو هذا المشروع؟ (What is this project?)
نظام متكامل يساعد المتقدمين للعمل في **JoAcademy** على رفع سيرهم الذاتية (CV) ومطابقتها فوراً مع الوظائف المتاحة حالياً باستخدام الذكاء الاصطناعي، مع تقديم تفسير منطقي لكل نتيجة مطابقة.

### 2. كيف يعمل النظام؟ (How it works?)
تمر العملية بأربعة مراحل أساسية:

#### أ. رفع واستخراج النص (Upload & Extraction)
عند رفع ملف الـ PDF، يقوم السيرفر باستخدام مكتبة `pdf-parse` باستخراج النص الخام من الملف. يتم تنظيف النص لضمان أفضل قراءة للذكاء الاصطناعي.

#### ب. جلب الوظائف الحية (Fetching Live Jobs)
يتصل النظام بـ API خاص بـ JoAcademy لجلب قائمة الوظائف المنشورة حالياً، مما يضمن أن النتائج دائماً "حية" وليست بيانات قديمة.

#### ج. محرك المطابقة الثنائي (Two-Stage AI Matching)
هذا هو "عقل" النظام، وينقسم لخطوتين لضمان السرعة والدقة:
1.  **المرحلة الأولى (Semantic Filtering)**: نستخدم تقنية **OpenAI Embeddings** لتحويل الـ CV والوظائف إلى متجهات رياضية. نقوم بحساب "التشابه الجيبي" (Cosine Similarity) لاختيار أفضل 10 وظائف محتملة بسرعة فائقة.
2.  **المرحلة الثانية (Deep Analysis)**: نرسل الـ CV مع الوظائف العشر المختارة إلى موديل **GPT-4o**. هنا يقوم الذكاء الاصطناعي بتمثيل دور "خبير توظيف" ليحلل المهارات، الخبرات السابقة، والتعليم بدقة متناهية.

#### د. التفسير والنتائج (Rationale & Results)
يطلب النظام من الـ AI كتابة "تفسير" (Rationale) لكل وظيفة. هذا يضمن الشفافية ويجعل المستخدم يعرف تماماً لماذا تم اختياره لهذا الدور.

---

### 🛠 التقنيات المستخدمة (Technical Stack)

| الجزء | التقنية |
| :--- | :--- |
| **واجهة المستخدم (Frontend)** | React, Vite, Tailwind CSS, Shadcn UI, Framer Motion |
| **السيرفر (Backend)** | Node.js, Express |
| **محرك الذكاء الاصطناعي (AI)** | OpenAI (GPT-4o & text-embedding-3-small) |
| **إدارة البيانات** | Zod (Validation), TanStack Query (State Management) |

---

### 🚀 كيفية التشغيل (How to Run)

1.  تأكد من وجود ملف `.env` يحتوي على مفتاح الـ API:
    `OPENAI_API_KEY=your_key_here`
2.  تثبيت المكتبات:
    `npm install`
3.  تشغيل وضع التطوير:
    `npm run dev`
4.  الدخول عبر المتصفح:
    `http://localhost:5000`

---

### 💡 لماذا هذا النظام أفضل؟
*   **دقة عالية**: لا يعتمد على الكلمات المفتاحية فقط (Keywords)، بل يفهم المعنى السياقي (Context).
*   **شفافية**: يخبرك بالسبب (Rationale) ولا يكتفي بالأرقام.
*   **مباشر**: مرتبط بالوظائف الحقيقية لـ JoAcademy لحظة بلحظة.

---
*تم إعداد هذا المستند لمساعدة فريق التطوير والمستخدمين في فهم النظام.*
